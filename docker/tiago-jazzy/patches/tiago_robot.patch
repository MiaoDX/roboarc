diff --git a/tiago_bringup/launch/tiago_bringup.launch.py b/tiago_bringup/launch/tiago_bringup.launch.py
index e66d37c..ef52712 100644
--- a/tiago_bringup/launch/tiago_bringup.launch.py
+++ b/tiago_bringup/launch/tiago_bringup.launch.py
@@ -37,6 +37,7 @@ class LaunchArguments(LaunchArgumentsBase):
     has_screen: DeclareLaunchArgument = TiagoArgs.has_screen
     use_sim_time: DeclareLaunchArgument = CommonArgs.use_sim_time
     is_public_sim: DeclareLaunchArgument = CommonArgs.is_public_sim
+    gazebo_version: DeclareLaunchArgument = CommonArgs.gazebo_version
     namespace: DeclareLaunchArgument = CommonArgs.namespace


@@ -69,6 +70,7 @@ def declare_actions(
             "base_type": launch_args.base_type,
             "use_sim_time": launch_args.use_sim_time,
             "is_public_sim": launch_args.is_public_sim,
+            "gazebo_version": launch_args.gazebo_version,
         },
     )

@@ -113,6 +115,7 @@ def declare_actions(
             "namespace": launch_args.namespace,
             "use_sim_time": launch_args.use_sim_time,
             "is_public_sim": launch_args.is_public_sim,
+            "gazebo_version": launch_args.gazebo_version,
         },
     )

diff --git a/tiago_bringup/launch/twist_mux.launch.py b/tiago_bringup/launch/twist_mux.launch.py
index 67950e6..fde94b1 100644
--- a/tiago_bringup/launch/twist_mux.launch.py
+++ b/tiago_bringup/launch/twist_mux.launch.py
@@ -64,7 +64,7 @@ def declare_actions(
     twist_mux = include_scoped_launch_py_description(
         'twist_mux', ['launch', 'twist_mux_launch.py'],
         launch_arguments={
-            'cmd_vel_out': 'mobile_base_controller/cmd_vel_unstamped',
+            'cmd_vel_out': 'mobile_base_controller/cmd_vel',
             'config_locks': config_locks_file,
             'config_topics': config_topics_file,
             "config_joy": LaunchConfiguration("config_joy"),
diff --git a/tiago_description/launch/robot_state_publisher.launch.py b/tiago_description/launch/robot_state_publisher.launch.py
index 5458ea3..cdc24f5 100644
--- a/tiago_description/launch/robot_state_publisher.launch.py
+++ b/tiago_description/launch/robot_state_publisher.launch.py
@@ -46,6 +46,7 @@ class LaunchArguments(LaunchArgumentsBase):

     use_sim_time: DeclareLaunchArgument = CommonArgs.use_sim_time
     is_public_sim: DeclareLaunchArgument = CommonArgs.is_public_sim
+    gazebo_version: DeclareLaunchArgument = CommonArgs.gazebo_version
     namespace: DeclareLaunchArgument = CommonArgs.namespace


@@ -111,6 +112,7 @@ def create_robot_description_param(context, *args, **kwargs):
         "has_screen": read_launch_argument("has_screen", context),
         "use_sim_time": read_launch_argument("use_sim_time", context),
         "is_public_sim": read_launch_argument("is_public_sim", context),
+        "gazebo_version": read_launch_argument("gazebo_version", context),
         "namespace": read_launch_argument("namespace", context),
     }

diff --git a/tiago_description/robots/tiago.urdf.xacro b/tiago_description/robots/tiago.urdf.xacro
index f199c69..9b29ded 100644
--- a/tiago_description/robots/tiago.urdf.xacro
+++ b/tiago_description/robots/tiago.urdf.xacro
@@ -68,6 +68,7 @@
   <xacro:arg name="is_multiple" default="false"/>
   <xacro:arg name="namespace" default=""/>
   <xacro:arg name="is_public_sim" default="false"/>
+  <xacro:arg name="gazebo_version" default="classic"/>

   <!-- PROPERTIES -->

@@ -128,6 +129,7 @@
   <xacro:property name="is_multiple" value="$(arg is_multiple)" />
   <xacro:property name="namespace" value="$(arg namespace)" />
   <xacro:property name="is_public_sim" value="$(arg is_public_sim)" />
+  <xacro:property name="gazebo_version" value="$(arg gazebo_version)" />

   <!-- INCLUDES -->

@@ -171,7 +173,7 @@

   <!-- Base -->
   <xacro:if value="${base_type == 'pmb2'}">
-    <xacro:base_sensors name="base" laser_model="${laser_model}" has_sonars="true" has_microphone="true"/>
+    <xacro:base_sensors name="base" laser_model="${laser_model}" has_sonars="true" has_microphone="true" gazebo_version="${gazebo_version}"/>
   </xacro:if>
   <xacro:if value="${base_type == 'omni_base'}">
     <xacro:base_sensors name="base" front_laser_model="${laser_model}" rear_laser_model="${laser_model}"/>
diff --git a/tiago_description/ros2_control/ros2_control.urdf.xacro b/tiago_description/ros2_control/ros2_control.urdf.xacro
index dca6a60..a63bf0d 100644
--- a/tiago_description/ros2_control/ros2_control.urdf.xacro
+++ b/tiago_description/ros2_control/ros2_control.urdf.xacro
@@ -55,11 +55,20 @@

     <!-- Init the ROS 2 control gazebo interface -->
     <xacro:if value="${use_sim_time}">
-      <gazebo>
-        <plugin filename="libgazebo_ros2_control.so" name="gazebo_ros2_control">
-          <parameters>$(find tiago_description)/ros2_control/gazebo_controller_manager_cfg.yaml</parameters>
-        </plugin>
-      </gazebo>
+      <xacro:if value="${gazebo_version == 'classic'}">
+        <gazebo>
+          <plugin filename="libgazebo_ros2_control.so" name="gazebo_ros2_control">
+            <parameters>$(find tiago_description)/ros2_control/gazebo_controller_manager_cfg.yaml</parameters>
+          </plugin>
+        </gazebo>
+      </xacro:if>
+      <xacro:if value="${gazebo_version == 'gazebo'}">
+        <gazebo>
+          <plugin filename="libgz_ros2_control-system.so" name="gz_ros2_control::GazeboSimROS2ControlPlugin">
+            <parameters>$(find tiago_description)/ros2_control/gazebo_controller_manager_cfg.yaml</parameters>
+          </plugin>
+        </gazebo>
+      </xacro:if>
     </xacro:if>

     <!-- ROS 2 control config-->
@@ -68,7 +77,12 @@
       <!-- Execution env config -->
       <hardware>
         <xacro:if value="${use_sim_time}">
-          <plugin>gazebo_ros2_control/GazeboSystem</plugin>
+          <xacro:if value="${gazebo_version == 'classic'}">
+            <plugin>gazebo_ros2_control/GazeboSystem</plugin>
+          </xacro:if>
+          <xacro:if value="${gazebo_version == 'gazebo'}">
+            <plugin>gz_ros2_control/GazeboSimSystem</plugin>
+          </xacro:if>
         </xacro:if>
         <xacro:unless value="${use_sim_time}">
           <plugin>robot_control/RobotControl</plugin>
