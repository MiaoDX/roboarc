#!/usr/bin/env bash
set -euo pipefail

repo_root=$(git rev-parse --show-toplevel)
cd "$repo_root"

if [[ ! -x .venv/bin/python ]]; then
  python -m venv .venv
  .venv/bin/python -m pip install -e ".[dev]"
fi
